export enum OrderType {
  SellGold = 'sell_gold',
}

export enum OrderStatus {
  Pending = 'pending',
  Completed = 'completed',
  Canceled = 'canceled',
}

export enum TransferType {
  MBBANK = 'mbbank',
}
